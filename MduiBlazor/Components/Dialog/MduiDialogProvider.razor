@namespace MduiBlazor

<div class="mdui-dialog-provider">
    <CascadingValue Value="@this" IsFixed>
        @foreach (var dialog in _dialogs)
        {
            @dialog.Instance
        }
    </CascadingValue>
</div>