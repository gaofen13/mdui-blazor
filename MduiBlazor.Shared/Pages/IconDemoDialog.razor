<MduiDialog Options="@(new DialogOptions{Modal=true})">
    <MduiIcon Icon="@IconName" />
    <CodeSnippet Style="border-radius: 12px;">@($"<MduiIcon Icon=\"{IconName}\"") /></CodeSnippet>
</MduiDialog>

@code {
    [Parameter]
    public string? IconName { get; set; }
}